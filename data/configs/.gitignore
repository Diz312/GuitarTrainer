# Configuration files directory
# Store application configuration files here
#
# Only default configurations are tracked in version control
# User-specific configurations are gitignored

# Ignore all config files except defaults
*.yaml
*.json
*.ini
*.cfg

# Keep default configuration
!default_config.yaml
!.gitkeep
